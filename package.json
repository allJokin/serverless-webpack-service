{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "yarn workspace my-package build",
    "predeploy": "yarn build",
    "deploy": "yarn workspace serverless-webpack-service deploy"
  }
}
